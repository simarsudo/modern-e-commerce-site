import React from "react";

const Navbar = () => {
	return (
		<div className="fixed top-0 left-0 z-40 h-12 w-full bg-rose-500 font-bold text-white">
			Navbar
		</div>
	);
};

export default Navbar;
